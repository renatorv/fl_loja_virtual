import 'package:flutter/material.dart';

class ComprasDetalhePage extends StatelessWidget {
  static String tag = '/compras-detalhe-page';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detalhes Page'),
      ),
      body: Container(),
    );
  }
}
